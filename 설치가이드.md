# 필름 카메라 — 안드로이드 앱 빌드 가이드

이 폴더를 GitHub에 올리면, GitHub이 자동으로 APK(설치 파일)를 만들어줍니다.
컴퓨터에 아무것도 설치할 필요 없습니다.

## 1. 새 저장소 만들기
- github.com → 오른쪽 위 [+] → New repository
- 이름: `film-camera-app` / **Public** 선택 → Create repository

## 2. 파일 올리기
- "uploading an existing file" 클릭
- 압축 푼 폴더 안의 **모든 파일과 폴더**를 한꺼번에 드래그
  (android, www, .github 폴더 + package.json 등 전부)
- Commit changes

## 3. (확인) Actions 탭
- 저장소 상단 **Actions** 탭 → "APK 빌드"가 노란색으로 돌아가는 중이면 성공
- 처음이면 "I understand... enable them" 버튼을 눌러 활성화
- ⚠️ 만약 Actions에 아무것도 없다면 `.github` 폴더가 업로드에서 빠진 것:
  Add file → Create new file → 파일 이름에 `.github/workflows/build.yml` 입력
  → 압축 폴더 안의 같은 파일 내용을 복사해 붙여넣고 Commit

## 4. APK 다운로드 (빌드 5~8분 소요)
- Actions 탭 → 초록색 체크가 뜬 빌드 클릭
- 아래 **Artifacts** 섹션 → `film-camera-apk` 클릭해 다운로드
- zip 안의 `app-debug.apk`를 폰으로 전송 (카톡 나에게 보내기 등)

## 5. 폰에 설치
- APK 파일 탭 → "출처를 알 수 없는 앱" 허용 → 설치
- 홈 화면에 **필름 카메라** 아이콘이 생깁니다!

## 이후 업데이트 방법
- www 폴더의 파일(index.html, app.js, style.css)만 갈아끼우고 커밋하면
  자동으로 새 APK가 빌드됩니다.
